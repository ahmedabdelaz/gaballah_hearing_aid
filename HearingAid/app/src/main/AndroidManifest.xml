<?xml version="1.0" encoding="utf-8"?>
<!--
    AndroidManifest.xml
    Declares all permissions, components, and hardware features.
    RECORD_AUDIO is mandatory — the OS will show a runtime permission dialog.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ── Permissions ───────────────────────────────────────────── -->

    <!-- Microphone access — required for real-time audio capture -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Modify audio settings (sample rate, buffer size) -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Bluetooth headset — many users wear BT hearing aids / earbuds -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"
        android:maxSdkVersion="30" />

    <!-- Keep CPU awake while processing audio (no screen needed) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Foreground service — lets the audio engine run when screen is off -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- ── Hardware features (not required, but declared for Play Store) -->
    <uses-feature android:name="android.hardware.microphone" android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HearingAid"
        tools:targetApi="34">

        <!-- Main activity — the UI entry point -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            AudioProcessingService — runs the AAudio engine as a foreground service.
            This keeps the JNI audio thread alive even when the app is backgrounded.
        -->
        <service
            android:name=".AudioProcessingService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

    </application>
</manifest>
